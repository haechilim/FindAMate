<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.findamate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindAMate">
        <activity android:name=".main.LogActivity"></activity>
        <activity
            android:name=".main.PopupStudentSettingActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".main.PopupClassSettingActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".main.PopupActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".main.SimulationActivity" />
        <activity android:name=".main.WaitingActivity" />
        <activity android:name=".main.SignupActivity" />
        <activity android:name=".main.MatchingActivity" />
        <activity android:name=".main.LoginActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>